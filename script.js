/*
function formatDate(date) {
  const options = {
    year: "numeric",
    month: "long",
    day: "numeric",
    hour: "2-digit",
    minute: "2-digit",
  };
  return date.toLocaleDateString("en-US", options);
}

document.querySelectorAll(".postDate").forEach((dateElement) => {
  dateElement.innerText = formatDate(new Date());
});
*/
